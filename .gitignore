# Dependencies
# Node.js module folder, where all packages are installed.
node_modules/

# Yarn Plug'n'Play (PnP) files for specific package manager configurations.
.pnp/
.pnp.js
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Test coverage reports generated by testing tools (like Jest/Vitest).
coverage/

# Next.js build outputs
# This is the primary build directory containing optimized bundles, cache, etc.
.next/

# Static HTML export directory (if you use 'next export')
out/

# Potential custom build/distribution directories
dist/
build/
standalone/

# Misc system files and IDE files
# Operating System temporary files
.DS_Store
Thumbs.db
*.pem

# IDE configuration files (VS Code, JetBrains IDEs)
.vscode/
.idea/

# Debug logs from various package managers
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-debug.log*

# Environment files (CRITICAL: Prevents sensitive keys from being committed)
# These files should contain secrets/API keys and must never be committed.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel deployment cache (local build artifacts from 'vc dev')
.vercel/

# TypeScript build cache and generated declaration file
# This improves incremental build speed but shouldn't be committed.
*.tsbuildinfo

# This file is generated by Next.js specifically for TypeScript integration.
next-env.d.ts

# Optional: Temporary output files (relevant to your previous issue)
# Add any specific log files generated by custom scripts:
tsc_output.txt