// Placeholder hero-parallax components to satisfy imports
"use client";

import React from "react";

export const HeroScrollMomentum = ({ children }: { children?: React.ReactNode }) => {
  return <div className="hero-scroll-momentum">{children}</div>;
};

export const HeroScrollParallax = ({ children }: { children?: React.ReactNode }) => {
  return <div className="hero-scroll-parallax">{children}</div>;
};

export const HeroScrollSnap = ({ children }: { children?: React.ReactNode }) => {
  return <div className="hero-scroll-snap">{children}</div>;
};

export const HeroScrollStep = ({ children }: { children?: React.ReactNode }) => {
  return <div className="hero-scroll-step">{children}</div>;
};

export const HeroScrollTreeShowcase = ({ children }: { children?: React.ReactNode }) => {
  return <div className="hero-scroll-tree-showcase">{children}</div>;
};

export const HeroScrollVerticalStory = ({ children }: { children?: React.ReactNode }) => {
  return <div className="hero-scroll-vertical-story">{children}</div>;
};
